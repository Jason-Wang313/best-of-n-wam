# LIBERO Tiny Neural VLA Attempt

- Attempted: `true`
- Verified: `false`
- Status: `timed_out`
- Timeout: `900` seconds
- Policy backend: `tiny_neural_vla`
- Suite: `libero_object`
- Tasks: all 10 Object task IDs
- Train seeds: `100`, `101`, `102`
- Eval seeds: `200`, `201`, `202`

The optional tiny neural RGB/proprio/language behavior-cloning path was added and attempted, but the full sparse-success run exceeded the time cap before producing a verified result. The existing verified kNN LIBERO visual-language BC artifact remains the canonical promoted sparse-success smoke.

This is blocker evidence, not a promoted modern-VLA result.
